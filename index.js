const Letter = require("./letter");
const Word = require("./word");
const inquirer = require("inquirer")

//use inquire to get letters from user



//Render word to terminal
console.log(Letter)
console.log(Word)

const newWord = new Word("five");

const guesses = letters.push(newWord)

console.log(guesses)

console.log(newWord)